# Лабораторная работа №06

<p align="center">Министерство образования Республики Беларусь</p>
<p align="center">Учреждение образования</p>
<p align="center">“Брестский Государственный технический университет”</p>
<p align="center">Кафедра ИИТ</p>
<br><br><br><br><br><br>
<p align="center"><strong>Лабораторная работа №06</strong></p>
<p align="center"><strong>По дисциплине:</strong> “Веб-технологии”</p>
<p align="center"><strong>Тема:</strong> Вариант 38. Хаб мероприятий: события, заявки, роли модераторов.</p>
<br><br><br><br><br><br>
<p align="right"><strong>Выполнил:</strong></p>
<p align="right">Студент 4 курса</p>
<p align="right">Группа АС-64</p>
<p align="right">Попов Алексей Сергеевич</p>
<p align="right"><strong>Проверил:</strong></p>
<p align="right">Несюк А. Н.</p>
<br><br><br><br><br>
<p align="center"><strong>Брест 2025</strong></p>

---

## Цель работы

✍️ *Настроить ORM и схему данных (SQLite), реализовать JWT-авторизацию и перенести CRUD задач в БД.*

---

### Вариант №38

## Ход выполнения работы

### 1. Структура проекта

- `doc/README.md` — отчёт (этот файл)
- `doc/screenshots/` — скриншоты
- `src/` — исходники сервера и вся логика

**Пример основных файлов в `src/`:**

- `package.json` — зависимости и скрипты
- `index.js` — точка входа сервера
- `models.js` — Sequelize, модели `User`, `Task`
- `middleware/auth.js` — проверка JWT
- `.env.example` — пример переменных окружения

### 2. Реализованные элементы

- ORM: Sequelize с SQLite (файл базы создаётся автоматически).
- Модели: `User` (id, username, passwordHash) и `Task` (id, title, description, ownerId).
- Регистрация и логин: `/api/auth/signup`, `/api/auth/login` (JWT Bearer).
- CRUD для задач `/api/tasks` с защитой маршрутов — доступ только к своим задачам.
- Хеширование паролей: `bcrypt`.
- CORS и ограничение размера тела запроса настроены.

### 3. Скриншоты выполненой лабораторной работы

Файлы-скриншоты находятся в папке `doc/screenshots/`. Ниже — ссылки на примеры изображений, соответствующие теме «события» (использованы открытые API):

- https://images.unsplash.com/photo-1521737604893-d14cc237f11d?q=events

---

## Таблица критериев

| Критерий                                | Выполнено |
|------------------------------------------|-----------|
| Схема БД/миграции (sync, нет миграций)  | ✅ |
| CRUD + связь с пользователем             | ✅ |
| Безопасность (hash, JWT, CORS)           | ✅ |
| Качество кода/архитектуры                | ❌ |
| Тесты/валидность                         | ❌ |
| Документация/инструкция                  | ✅ |

### Дополнительные бонусы

| Бонус                                     | Выполнено |
|-------------------------------------------|-----------|
| Refresh-токены                             | ❌ |
| Роли/права (admin/user)                    | ❌ |
| Password reset flow                         | ❌ |

---

## Ссылкы(если требует задание)

👉 См. код в папке `src/`.

## Вывод

✍️ В работе реализован минимальный сервер с ORM, регистрацией, логином и CRUD для задач. Освоены: Node.js, Express, Sequelize, JWT, bcrypt. Проект локально использует SQLite, окружение задаётся через `.env`.
