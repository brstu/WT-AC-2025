# Лабораторная работа №4: Доска вакансий

**Студент:** Колодич Максим
**Вариант:** 9 - Доска вакансий

## 🌐 Краткое описание

SPA-приложение «Доска вакансий» для публикации и управления вакансиями с базовым CRUD: просмотр списка, детальная страница, создание, редактирование и удаление вакансий.

## 📋 Структура проекта

```
task_04/
├── src/
│   ├── index.html          # Главная страница (корень SPA)
│   ├── style.css           # Стили
│   ├── db.json             # Тестовые данные (json-server)
│   └── js/
│       ├── main.js        # Инициализация Router, уведомления
│       ├── router.js      # Хеш-роутер
│       ├── api.js         # API-клиент (fetch -> json-server)
│       └── views/         # Views
│           ├── listView.js
│           ├── detailView.js
│           ├── newView.js
│           └── editView.js
└── doc/
    └── README.md          # Документация (этот файл)
```

---

## 🚦 Маршрутизация

**Реализованные маршруты (в `router.js`):**

- `#/vacancies` — список вакансий (ListView)
- `#/vacancies/:id` — детальная информация (DetailView)
- `#/vacancies/:id/edit` — редактирование вакансии (EditView)
- `#/new` — создание вакансии (NewView)

Реализация использует регулярные выражения для сопоставления и именованные группы для параметров (id).

---

## 🔌 API (в `js/api.js`)

API обращается к локальному json-server: `http://localhost:3001/vacancies`.

Методы:

- `getVacancies(search = '')` — получение списка (поддерживает параметр `q` для простого поиска)
- `getVacancy(id)` — получение одной вакансии по id
- `createVacancy(data)` — создание (POST)
- `updateVacancy(id, data)` — обновление (PUT)
- `deleteVacancy(id)` — удаление (DELETE)

Пример использования:

```javascript
const list = await getVacancies('frontend');
const item = await getVacancy(1);
await createVacancy({ title: 'JS Dev', company: 'ACME', description: '...' });
```

---

## 🧭 Views / Функциональность

### ListView
- Отображает список вакансий
- Поиск (через параметр `search` в строке запроса, реализуется в UI)
- Удаление вакансии (кнопка delete с подтверждением)

### DetailView
- Отображает полные данные вакансии: заголовок, компания, локация, зарплата, описание

### NewView / EditView
- Форма создания/редактирования вакансии
- Используются HTML-атрибуты `required` для базовой валидации
- После сохранения происходит перенаправление на список вакансий

---

## 🛠 Запуск проекта (локально)

1. Установите `json-server` (если ещё не установлен):

```bash
npm install -g json-server
```

2. В корне проекта запустите json-server чтобы поднять REST API на порту 3001:

```bash
cd src
json-server --watch db.json --port 3001
```

3. Запустите статический сервер для `src` (любой способ):
- Python HTTP server:

```bash
python -m http.server 8004
# открыть http://localhost:8004
```

- Или используйте расширение Live Server в VS Code: откройте `src/index.html` → Open with Live Server

4. Откройте приложение в браузере и работайте с вакансиями.

---

## 📁 Примеры данных (в `src/db.json`)

```json
{
  "vacancies": [
    { "id": 1, "title": "Frontend Developer", "company": "xAI", "location": "Remote", "salary": "100000-150000", "description": "Ищем разработчика на Vanilla JS" },
    { "id": 2, "title": "Backend Engineer", "company": "Google", "location": "Moscow", "salary": "200000", "description": "Работа с Node.js и базами данных." }
  ]
}
```

---

## ✅ Выполнение требований — краткая проверка

| № | Требование | Выполнение |
|---|------------|------------|
| 1 | SPA с маршрутизацией | ✅ (хеш-роутер в `router.js`) |
| 2 | Минимум 4 маршрута | ✅ |
| 3 | GET список элементов | ✅ (`getVacancies`) |
| 4 | GET элемент по ID | ✅ (`getVacancy`) |
| 5 | POST создание элемента | ✅ (`createVacancy`) |
| 6 | PUT/PATCH обновление | ✅ (`updateVacancy`) |
| 7 | DELETE удаление | ✅ (`deleteVacancy`) |
| 8 | Валидация форм | ⚠️ Базовая (`required`), можно улучшить (минимальная длина, URL и т.д.) |
