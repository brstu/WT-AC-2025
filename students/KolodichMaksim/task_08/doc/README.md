# Course Catalog — Lab 08

> **Testing, Docker, CI/CD и аудит качества**

**Студент:** Колодич Максим Павлович
**Лабораторная работа:** 09 — Каталог курсов

---

## 🌐 Live Demo

🔗 **(Если есть деплой — добавьте ссылку сюда)**

---

## 📋 Описание проекта

Приложение — каталог онлайн-курсов с возможностью поиска, фильтрации и просмотра деталей курса. Проект реализован на React + TypeScript и использует Vite как сборщик.

### Основные возможности

- Просмотр списка курсов на главной странице
- Страница курса с подробной информацией
- Поиск и фильтрация (компонент `SearchFilter`)
- Карточки курса (`CourseCard`) и список (`CourseList`)
- E2E тесты (Playwright)
- CI/CD: GitHub Actions с прогоном тестов, сборкой и Lighthouse
- Dockerfile и nginx-конфигурация (в `src/` — рекомендуется перенести в корень проекта)

---

## 🚀 Быстрый старт

```bash
# Установить зависимости
npm install

# Локальная разработка
npm run dev
# ➡️ Откройте http://localhost:5173

# Production сборка
npm run build
npm run preview
```

### Тестирование

```bash
# E2E (Playwright)
npm run e2e

# Unit tests (Vitest) — в проекте нет/мало unit тестов; рекомендуется добавить
```

---

## 📁 Структура проекта (основные файлы)

```
task_08/
├── src/
│   ├── App.tsx
│   ├── main.tsx
│   ├── pages/
│   │   ├── Home.tsx
│   │   └── CoursePage.tsx
│   ├── components/
│   │   ├── CourseCard.tsx
│   │   ├── CourseDetails.tsx
│   │   ├── CourseList.tsx
│   │   └── SearchFilter.tsx
│   ├── data/courses.json
│   ├── Dockerfile
│   └── nginx.conf
├── tests/
│   └── e2e/catalog.spec.ts
├── .github/workflows/ci.yml
├── package.json
└── vite.config.ts
```

---

## ✅ Текущий статус и рекомендации

| Критерий | Статус | Рекомендация |
|---|---:|---|
| E2E (Playwright) | ✅ | Есть 2 сценария в `tests/e2e/catalog.spec.ts` |
| Unit / Integration (Vitest) | ⚠️ | Unit тесты не найдены — добавить тесты для компонентов и утилит |
| CI/CD | ✅ | `.github/workflows/ci.yml` запускает тесты, сборку и Lighthouse CI |
| Docker | ⚠️ | `Dockerfile` находится в `src/` — лучше перенести в корень и добавить `docker-compose.yml` для локального запуска |
| Lighthouse / Audit | N/A | GitHub Action настроен для запуска Lighthouse CI — требуется проверка результатов |
| Документация | ⚠️ | Рекомендуется добавить `REPORT.md` и `DEPLOYMENT.md` с результатами тестов и инструкциями по деплою |

---

## 🛠 Технологии

- React + TypeScript
- Vite
- Playwright (E2E)
- GitHub Actions (CI)
- Docker + Nginx
- ESLint

---

## 📋 Доступные команды (из `package.json`)

```bash
npm run dev       # Dev server (vite)
npm run build     # Production build
npm run preview   # Preview production build
npm run e2e       # Playwright E2E tests
npm run test      # Vitest (если добавите тесты)
npm run lint      # ESLint
```

---

## ✅ Рекомендации по доработке

- Добавить unit/integration тесты (Vitest + Testing Library) для `CourseList`, `CourseCard`, `SearchFilter`.
- Перенести `Dockerfile` в корень проекта и создать `docker-compose.yml` для локального окружения.
- Добавить `REPORT.md` с результатами тестов, Lighthouse и скриншотами в `doc/screenshots/`.
- При наличии деплоя — добавить ссылку Live Demo.
