# Отчёт по проекту: Конструктор To-Do + UI компоненты

## 📊 Общая информация

**Название проекта:** Конструктор To-Do с UI компонентами  
**Технологии:** HTML5, CSS3, Vanilla JavaScript (ES6+)  
**Подход:** Mobile-first, Accessibility-first  
**Спецификация:** WCAG 2.1, ARIA 1.2

## 🎯 Реализованные компоненты

### 1. 📋 **To-Do Список с фильтрами**
- **Функции:**
  - Добавление задач с валидацией
  - Drag-n-drop сортировка (перетаскивание)
  - Лайки/избранное для задач
  - Модальное подтверждение удаления
  - Фильтрация через табы: Все / Любимые / Остальные
- **Доступность:**
  - Keyboard navigation (Tab, Space, Enter)
  - ARIA labels для всех элементов
  - Focus management при открытии модалки

### 2. 🗂️ **Табы (фильтры)**
- **Функции:**
  - Три состояния: Все / Любимые / Остальные
  - Клавиатурная навигация: ← → стрелки
  - Автоматическое обновление списков
- **Доступность:**
  - `role="tablist"`, `role="tab"`, `role="tabpanel"`
  - `aria-selected`, `aria-controls`
  - Правильная фокусировка

### 3. 📖 **Аккордеон**
- **Функции:**
  - Раскрытие/скрытие секций
  - Независимое управление каждой секцией
  - Плавная анимация
- **Доступность:**
  - `aria-expanded`, `aria-controls`
  - Клавиатурное управление (Enter/Space)

### 4. 📝 **Контактная форма**
- **Функции:**
  - Валидация в реальном времени
  - Проверка email (регулярные выражения)
  - Минимальная длина сообщения (20 символов)
  - Динамическая блокировка кнопки отправки
- **Доступность:**
  - ARIA live regions для ошибок
  - `aria-required`, `aria-invalid`
  - Screen reader announcements

### 5. 🍔 **Адаптивное меню (бургер)**
- **Функции:**
  - Автоматическое скрытие на мобильных
  - Плавное открытие/закрытие
  - Правильное управление фокусом
- **Доступность:**
  - `aria-expanded`, `aria-controls`
  - `aria-label` для кнопки меню

### 6. ⚠️ **Модальное окно подтверждения**
- **Функции:**
  - Открытие при удалении задачи
  - Блокировка фона
  - Закрытие по Esc и клику вне
- **Доступность:**
  - `aria-modal="true"`
  - Ловушка фокуса внутри модалки
  - Правильное описание

### 7. 💡 **Tooltip (подсказки)**
- **Функции:**
  - Показ подсказок при фокусе
  - Автоматическое позиционирование
  - Умное скрытие
- **Доступность:**
  - `role="tooltip"`
  - Программное управление

## ♿ Доступность (A11Y)

### Клавиатурная навигация
| Компонент | Клавиши | Действие |
|-----------|---------|----------|
| Навигация | Tab/Shift+Tab | Переход по ссылкам |
| Табы | ← → / Tab | Переключение вкладок |
| Аккордеон | Enter/Space | Раскрытие/скрытие |
| Модалка | Esc | Закрытие |
| Drag handle | Enter/Space | Показать подсказку |

### ARIA Атрибуты
```html
<!-- Табы -->
<div role="tablist" aria-label="Фильтры задач">
<button role="tab" aria-selected="true" aria-controls="panel-all">

<!-- Аккордеон -->
<button aria-expanded="false" aria-controls="acc-panel-1">

<!-- Форма -->
<input aria-required="true" aria-invalid="false">
<span role="alert" aria-live="polite"></span>

<!-- Модалка -->
<div role="dialog" aria-modal="true" aria-labelledby="confirmTitle">
```
Screen Reader поддержка
Все изображения имеют alt тексты

Скрытые элементы правильно скрыты (aria-hidden, hidden)

Логическая структура заголовков (h1-h3)

Описательные labels для форм

📱 Адаптивный дизайн
Брейкпоинты
Устройство	Ширина	Особенности
Мобильные	< 768px	Бургер-меню, 1 колонка
Планшеты/Десктоп	≥ 768px	Горизонтальное меню, мультиколоночность
Медиазапросы
```css
/* Mobile-first (базовые стили) */
.main-nav { display: none; }
.burger { display: inline-flex; }

/* Desktop */
@media (min-width: 768px) {
    .main-nav { display: block; }
    .burger { display: none; }
}
Grid/Flexbox системы
css
/* To-Do форма */
.todo-form {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: .5rem;
}

/* Список задач */
.todo-list {
    display: grid;
    gap: .5rem;
}

/* To-Do элемент */
.todo-item {
    display: grid;
    grid-template-columns: auto 1fr auto auto;
    align-items: center;
    gap: .5rem;
}
🎨 Дизайн-система
Цветовая палитра (CSS Custom Properties)
css
:root {
    --bg: #0f1115;            /* Основной фон */
    --card: #171a21;          /* Фон карточек */
    --text: #e7e9ee;          /* Основной текст */
    --muted: #a8b0c2;         /* Второстепенный текст */
    --primary: #4f8cff;       /* Основной акцент (синий) */
    --danger: #ff4f64;        /* Опасные действия (красный) */
    --success: #2ecc71;       /* Успех (зелёный) */
    --border: #2a2f3a;        /* Границы */
    --focus: #ffd166;         /* Цвет фокуса (жёлтый) */
}
```

Шрифт: System UI stack (системные шрифты)

Базовая линия: 1.5 (оптимальная читаемость)

Размеры: Отзывчивые, увеличиваются на десктопе

⚡ JavaScript Архитектура
Модульная структура
```javascript
// Утилиты
const qs = (sel, root = document) => root.querySelector(sel);
const qsa = (sel, root = document) => [...root.querySelectorAll(sel)];
```
// Компоненты
- Бургер-меню
- Табы
- Аккордеон
- Tooltip
- To-Do список
- Модалка
- Валидация формы
State Management
```javascript
// Глобальное состояние
let todos = []; // Массив объектов { id, text, liked }
let dragSrcId = null; // Для drag-n-drop
let pendingDeleteId = null; // Для модалки
Event Delegation
```
```javascript
// Единый обработчик для списков
[listAll, listLiked, listUnliked].forEach(container => {
    container.addEventListener('click', (e) => {
        const btn = e.target.closest('button');
        if (!btn) return;
        
        const li = e.target.closest('.todo-item');
        const id = li.dataset.id;
        const action = btn.dataset.action;
        
        if (action === 'like') toggleLike(id);
        if (action === 'delete') openConfirmModal(id);
    });
});
Drag-n-drop реализация
```
```javascript
// Полный цикл событий
li.addEventListener('dragstart', handleDragStart);
li.addEventListener('dragend', handleDragEnd);
li.addEventListener('dragover', handleDragOver);
li.addEventListener('dragleave', handleDragLeave);
li.addEventListener('drop', handleDrop);
```
```javascript
// Переупорядочивание массива
function reorderTodos(srcId, dstId) {
    const fromIdx = todos.findIndex(t => t.id === srcId);
    const toIdx = todos.findIndex(t => t.id === dstId);
    const [moved] = todos.splice(fromIdx, 1);
    todos.splice(toIdx, 0, moved);
    renderLists();
}
Валидация формы
javascript
// Реактивная валидация
['input', 'blur'].forEach(evt => {
    nameInput.addEventListener(evt, () => {
        validateName();
        updateSubmitState();
    });
});

// Обновление состояния кнопки
function updateSubmitState() {
    const valid = validateName() & validateEmail() & validateMessage();
    submitBtn.disabled = !Boolean(valid);
}
