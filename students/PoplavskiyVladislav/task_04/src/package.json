{
  "name": "esports-directory-lr04",
  "version": "1.0.0",
  "scripts": {
    "start:api": "json-server --watch db.json --port 3000",
    "start": "npx http-server -c-1 -p 8080"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "http-server": "^14.1.1"
  }
}
