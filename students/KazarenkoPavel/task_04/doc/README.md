# 🍳 Лабораторная работа №4: Справочник рецептов (SPA)

Одностраничное приложение на чистом JavaScript с hash-маршрутизацией и полным CRUD функционалом для управления рецептами.

## 📋 Функциональность

### ✅ Основные возможности:

- **Просмотр списка рецептов** с пагинацией и фильтрацией
- **Детальный просмотр рецепта** со всеми ингредиентами и шагами
- **Создание новых рецептов** через интуитивную форму
- **Редактирование существующих рецептов**
- **Удаление рецептов** с подтверждением
- **Поиск и фильтрация** по категории, сложности, времени

### ✅ Маршрутизация (hash-based):

- `#/recipes` - список всех рецептов
- `#/recipes/:id` - детали рецепта
- `#/recipes/new` - создание нового рецепта
- `#/recipes/:id/edit` - редактирование рецепта

### ✅ Состояния интерфейса:

- Loading (скелетон загрузки)
- Error (обработка ошибок с кнопкой повтора)
- Empty (пустые состояния с подсказками)
- Success (уведомления об успешных операциях)

### ✅ CRUD операции:

- **GET** `/recipes` - получение списка
- **GET** `/recipes/:id` - получение одного рецепта
- **POST** `/recipes` - создание рецепта
- **PATCH** `/recipes/:id` - обновление рецепта
- **DELETE** `/recipes/:id` - удаление рецепта

## 🛠 Технологии

- **Vanilla JavaScript (ES6+)** - без фреймворков
- **CSS Grid/Flexbox** - адаптивная верстка
- **Hash Router** - собственная реализация роутера
- **Fetch API** с обработкой ошибок
- **LocalStorage** для токенов авторизации
- **My JSON Server** для mock API

## 🔧 Локальный запуск

### 1. Установка зависимостей

```bash
# Для работы с mock API (опционально)
npm install -g json-server
```

### 2. Запуск mock сервера

```bash
json-server --watch mock/db.json --port 3000 --delay 1000
```

### 3. Запуск приложения

Откройте index.html в браузере или используйте live server:

```bash
npx live-server
```

### 4. Настройка API

По умолчанию приложение использует My JSON Server. Для локальной работы измените в js/app.js:

```javascript
this.api = new RecipesAPI('https://my-json-server.typicode.com/catsker/recipes-api-lab4');
```
