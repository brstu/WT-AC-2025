# Доска объявлений - React SPA

SPA приложение для размещения и поиска объявлений с авторизацией и ролевой системой.

## Быстрый старт

1. Клонируйте репозиторий
2. Установите зависимости: `npm install`
3. Настройте переменные окружения в `.env`
4. Запустите сервер разработки: `npm run dev`
5. Откройте http://localhost:3000

## Основные функции

- 📝 Создание, редактирование, удаление объявлений
- 🔍 Поиск и фильтрация по категориям
- 👤 Регистрация и авторизация пользователей
- 📱 Адаптивный дизайн
- 🖼️ Загрузка изображений
- 🔄 Оптимистичные обновления
- 📊 Пагинация

## Технологии

- React 18 + TypeScript
- React Router v6 (ленивая загрузка)
- Redux Toolkit + RTK Query
- React Hook Form + Zod валидация
- Tailwind CSS
- Vite
- Vitest + Testing Library

## Структура проекта

## Доступные страницы

- `/` - Главная (все объявления)
- `/ad/:id` - Детали объявления
- `/login` - Вход
- `/register` - Регистрация
- `/create` - Создание объявления
- `/edit/:id` - Редактирование объявления
- `/profile` - Профиль пользователя
- `/my-ads` - Мои объявления

## API интеграция

Приложение использует REST API с базовым URL из переменной окружения `VITE_API_URL`.

## Тестирование

```bash
# Запуск тестов
npm test

# Запуск тестов с UI
npm run test:ui
```
