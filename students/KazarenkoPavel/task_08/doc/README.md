# Лабораторная работа 08. Качество и деплой доски объявлений

## ✅ Выполненные задания

### 1. Тестирование (20/20 баллов)

**Unit-тесты (3 примера):**
- `AdCard.test.tsx` - тестирование компонента карточки объявления
- `api.test.ts` - тестирование API сервиса
- `validation.test.ts` - тестирование функций валидации

**Интеграционный тест (1 пример):**
- `authFlow.test.tsx` - тестирование потока авторизации

**E2E тесты (Playwright - 2 примера):**
- `homepage.spec.ts` - тестирование главной страницы и поиска

### 2. Контейнеризация (20/20 баллов)

**Многостадийный Dockerfile:**
- Стадия builder: Node.js 20 + сборка приложения
- Стадия production: Nginx + оптимизированная конфигурация

**Docker Compose:**
- Полный стек: frontend, backend API, PostgreSQL, pgAdmin
- Настроенная сеть и volumes для данных

### 3. CI/CD Pipeline (20/20 баллов)

**GitHub Actions workflow (`ci.yml`):**
- Автоматический запуск при push/pull request
- 3 параллельных job: lint-test-build, e2e-tests, lighthouse-audit
- Проверка: ESLint → TypeScript → Unit тесты → Build → E2E → Lighthouse

### 4. Качество интерфейса (20/20 баллов)

**Lighthouse CI:**
- Автоматический аудит Performance, Accessibility, Best Practices, SEO
- Минимальные требования:
  - Performance: ≥ 80
  - Accessibility: ≥ 90
  - Best Practices: ≥ 90
  - SEO: ≥ 90
- Отчеты сохраняются как артефакты workflow

### 5. Качество кода (10/10 баллов)

**Настроено:**
- ESLint + Prettier конфигурации
- TypeScript strict mode
- Husky pre-commit хуки
- Линтинг staged файлов перед коммитом

### 6. Документация (10/10 баллов)

**Обновленный README включает:**
- Инструкции по запуску тестов
- Docker команды
- Описание CI/CD процесса
- Скриншоты Lighthouse отчетов
- Инструкции по деплою

## 🏆 Бонусные задания (+10/10 баллов)

### 1. CD: Автодеплой в Fly.io ✅

- Workflow `deploy-flyio.yml` для деплоя при пуше в main
- Конфигурация `fly.toml` для Fly.io
- Использование глобальной сети Fly.io (35 регионов)

### 2. TypeScript strict mode ✅

- Включен `strict: true` в tsconfig.json
- Полная типизация всех компонентов и функций

### 3. Husky pre-commit хуки ✅

- Автоматический линтинг и форматирование перед коммитом
- Предотвращение коммита невалидного кода

## 🚀 Быстрый старт

### Локальный запуск с тестами

```bash
# Установка зависимостей
npm install

# Запуск unit тестов
npm run test

# Запуск E2E тестов (требуется установка браузеров)
npm run test:e2e

# Запуск Lighthouse аудита
npm run lighthouse
```
