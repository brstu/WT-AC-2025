# Mini Shop - React SPA

React SPA для мини-магазина с каталогом товаров, деталями, корзиной и мок-API.

## 🚀 Live Demo

**[https://task07-sigma.vercel.app](https://task07-sigma.vercel.app)**

## Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Запуск тестов
npm test

# Запуск тестов с UI интерфейсом
npm run test:ui
```

## Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
VITE_API_URL=http://localhost:3000/api
```

## Возможности

- �️ Каталог товаров с поиском и фильтрацией по категориям
- 📋 Детальная страница товара с описанием, рейтингом и управлением количеством
- 🛒 Корзина с управлением количеством товаров и расчетом суммы
- 💾 Сохранение корзины в localStorage
- 🎨 Тёмная и светлая тема с переключением
- 📱 Адаптивный дизайн (мобильный-первый подход)
- 📄 Пагинация товаров
- 🧪 Unit тесты с Vitest

## Технологический стек

- **Frontend Framework:** React 19.2
- **Build Tool:** Vite 7.2
- **State Management:** Redux Toolkit + RTK Query
- **Routing:** React Router v7
- **Forms:** React Hook Form + Zod
- **Testing:** Vitest + React Testing Library (39 passing tests)
- **Type Validation:** PropTypes
- **Styling:** CSS Modules
- **Deployment:** Vercel

## Структура проекта

```
src/
├── components/       # Переиспользуемые компоненты
│   ├── common/      # Navbar, Notification и др.
│   └── ui/          # Button, Card, Input, Spinner
├── features/        # Feature-based модули
│   ├── auth/        # Аутентификация
│   └── shop/        # Интернет-магазин
│       ├── api/     # RTK Query endpoints
│       ├── components/ # ProductCard
│       └── pages/   # ProductsListPage, ProductDetailPage, CartPage
├── layouts/         # Макеты страниц
├── pages/           # Основные страницы
├── router/          # Конфигурация маршрутов
├── store/           # Redux store (слайсы)
└── test/            # Настройка тестов
```

## API маршруты

Используется мок-API через RTK Query с данными в памяти:

- `GET /products` - Получить товары (с поиском и фильтрацией)
- `GET /products/:id` - Получить товар по ID
- `GET /categories` - Получить категории товаров

## Маршруты SPA

- `/` - Каталог товаров (главная)
- `/products` - Каталог товаров
- `/products/:id` - Деталь товара
- `/cart` - Корзина
- `*` - 404 Not Found

## Команды

```bash
# Development
npm run dev          # Запуск dev-сервера
npm run build        # Production сборка
npm run preview      # Предпросмотр production

# Testing
npm test            # Запуск тестов
npm run test:ui     # Тесты с UI

# Code Quality
npm run lint        # Проверка ESLint
```

## Развёртывание

### Vercel

```bash
npm install -g vercel
vercel
```

### Netlify

```bash
npm run build
netlify deploy --prod --dir=dist
```

## Демонстрационные данные

Приложение использует мок-данные с 6 товарами в разных категориях:
- Electronics (Ноутбук, Смартфон, Планшет)
- Audio (Наушники)
- Wearables (Умные часы)
- Accessories (Веб-камера)

## Варианты задания

**Вариант №10: Мини-магазин (фронт): список/деталь/корзина (мок-API)**

## Лицензия

MIT

## Автор

Краге Алина, АС-63
