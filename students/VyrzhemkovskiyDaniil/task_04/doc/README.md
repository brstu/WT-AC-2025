# Лабораторная работа 04. Вариант №2 — Библиотека фильмов (SPA)

## Описание

Одностраничное приложение (SPA) на чистом JavaScript без фреймворков. Каталог фильмов с полным набором CRUD операций, поиском и фильтрацией по жанрам. Данные хранятся в localStorage браузера, что обеспечивает работу без сервера.

## Функциональность

### 📋 **CRUD операции:**

- **Создание** — добавление новых фильмов в библиотеку
- **Чтение** — просмотр списка фильмов и детальной информации
- **Обновление** — редактирование существующих фильмов
- **Удаление** — удаление фильмов с подтверждением

### 🔍 **Навигация и поиск:**

- **Hash-маршрутизация** с поддержкой параметров
- **Поиск** по названию, режиссеру, описанию и жанрам
- **Фильтрация** по жанрам с сохранением состояния в URL
- **Адаптивный дизайн** для мобильных и десктопных устройств

### 🎨 **Пользовательский интерфейс:**

- **Уведомления** (Toast) о результатах операций
- **Валидация форм** с понятными сообщениями об ошибках
- **Подтверждение удаления** перед выполнением
- **Блокировка кнопок** во время отправки данных
- **Темная тема** с современным градиентным дизайном
- **Анимации** и плавные переходы

## Структура проекта

```bash
project/
├── index.html            # Главная HTML-страница
├── styles.css            # Стили приложения (темная тема с градиентами)
└── js/                   # JavaScript модули
    ├── api.js            # Модуль работы с данными (localStorage)
    ├── app.js            # Главный модуль приложения
    ├── router.js         # Hash-маршрутизатор
    ├── components/       # Переиспользуемые UI компоненты
    │   ├── Empty.js      # Компонент пустого состояния
    │   ├── Error.js      # Компонент ошибки
    │   ├── Loading.js    # Индикатор загрузки
    │   └── Toast.js      # Всплывающие уведомления
    └── views/            # Представления (страницы)
        ├── CreateView.js # Форма создания фильма
        ├── DetailView.js # Детальная информация о фильме
        ├── EditView.js   # Форма редактирования фильма
        └── ListView.js   # Список фильмов
```

## API (локальное хранилище)

Все методы возвращают Promise для имитации асинхронных операций с задержкой 300-500мс.

### 📚 **Основные методы:**

```javascript
// Получить все фильмы
movieApi.getList(): Promise<Movie[]>

// Получить фильм по ID
movieApi.getItem(id: string): Promise<Movie>

// Создать новый фильм
movieApi.create(data: MovieData): Promise<Movie>

// Обновить фильм
movieApi.update(id: string, data: MovieData): Promise<Movie>

// Удалить фильм
movieApi.remove(id: string): Promise<void>

// Поиск фильмов
movieApi.search(query: string): Promise<Movie[]>
```

### 🎬 **Структура данных фильма:**

```typescript
interface Movie {
  id: string;              // Уникальный идентификатор
  title: string;           // Название фильма
  director: string;        // Режиссер
  year: number;           // Год выпуска
  genre: string;          // Жанры через запятую
  rating: number;         // Рейтинг 0-10
  duration: string;       // Продолжительность
  description: string;    // Описание
  country: string;        // Страна производства
  language: string;       // Язык
  budget: string;         // Бюджет
  boxOffice: string;      // Сборы
  createdAt: string;      // Дата добавления
}
```

## Ключевые особенности реализации

### 🏗️ **Архитектура:**

- **Модульная структура** с разделением ответственности
- **Чистый JavaScript** без сторонних зависимостей
- **Hash-based routing** для SPA навигации
- **LocalStorage API** для хранения данных

### 🎯 **Функциональные компоненты:**

1. **ListView** — отображение сетки фильмов с поиском и фильтрацией
2. **DetailView** — детальная информация с метаданными и действиями
3. **CreateView** — форма добавления с валидацией
4. **EditView** — форма редактирования с предзаполнением

### 🛡️ **Валидация форм:**

- Обязательные поля: название и описание
- Проверка рейтинга (0-10)
- Проверка года выпуска (1888-текущий+5)
- Минимальная длина текстовых полей

### 🔧 **Технические детали:**

1. **Инициализация данных** — при первом запуске создаются тестовые фильмы
2. **Делегирование событий** — эффективная обработка кликов в списке
3. **Debounce поиска** — оптимизация производительности
4. **Асинхронные операции** — имитация сетевых запросов
5. **Обработка ошибок** — graceful degradation при ошибках

## Тестирование функциональности

### ✅ **Базовые сценарии:**

1. **Просмотр списка фильмов** (`#/movies`)
   - Проверка отображения карточек
   - Проверка наличия тестовых данных

2. **Поиск и фильтрация**
   - Поиск по названию, режиссеру, жанру
   - Фильтрация по жанрам через теги
   - Очистка фильтров

3. **Детальный просмотр** (`#/movies/:id`)
   - Переход по кнопке "Подробнее"
   - Отображение полной информации
   - Навигация "Назад"

4. **Создание фильма** (`#/new`)
   - Валидация обязательных полей
   - Проверка числовых полей
   - Уведомление об успешном добавлении

5. **Редактирование фильма** (`#/movies/:id/edit`)
   - Предзаполнение формы
   - Сохранение изменений
   - Возврат к детальному просмотру

6. **Удаление фильма**
   - Подтверждение действия
   - Уведомление об успешном удалении
   - Обновление списка

### 🧪 **Дополнительные проверки:**

- **Адаптивность** — проверка на разных размерах экрана
- **Состояние загрузки** — индикаторы при операциях
- **Обработка ошибок** — сообщения при недоступности данных
- **Доступность** — клавиша Esc для возврата
- **Состояние URL** — сохранение параметров поиска

## Установка и запуск

1. **Скопируйте все файлы** в одну папку
2. **Откройте `index.html`** в браузере
3. **Начните работу** — приложение готово к использованию

**Требования:** современный браузер с поддержкой ES6+ (Chrome 60+, Firefox 55+, Safari 11+, Edge 79+)

## Особенности реализации

### 🎨 **Дизайн:**

- Темная тема с акцентами фиолетового (#a78bfa)
- Градиенты и тени для глубины
- Анимации при наведении и переходах
- Отзывчивая сетка карточек

### ⚡ **Производительность:**

- Локальное хранение — мгновенный доступ к данным
- Оптимизированные рендеры — перерисовка только измененных частей
- Кэширование данных — минимизация чтения из localStorage

### 🛠️ **Расширяемость:**

- Легко добавить новые поля фильмов
- Поддержка дополнительных фильтров
- Возможность подключения к внешнему API
- Модульная структура для добавления новых view

## Решенные проблемы

1. **Навигация** — стабильная работа hash-роутинга
2. **Состояние данных** — согласованность между view
3. **Обратная совместимость** — поддержка старых браузеров
4. **UX/UI** — интуитивный интерфейс с обратной связью
5. **Обработка ошибок** — устойчивость к исключительным ситуациям

## Демонстрационные данные

При первом запуске автоматически создаются 3 демо-фильма:

1. "Начало" (2010) — Кристофер Нолан
2. "Криминальное чтиво" (1994) — Квентин Тарантино
3. "Побег из Шоушенка" (1994) — Фрэнк Дарабонт

Это позволяет сразу протестировать все функции приложения без необходимости добавления данных.
