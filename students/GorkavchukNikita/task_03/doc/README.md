# Министерство образования Республики Беларусь

<p align="center">Учреждение образования</p>
<p align="center">“Брестский Государственный технический университет”</p>
<p align="center">Кафедра ИИТ</p>
<br><br><br><br><br><br>
<p align="center"><strong>Лабораторная работа №3</strong></p>
<p align="center"><strong>По дисциплине:</strong> “Веб-технологии”</p>
<p align="center"><strong>Тема:</strong> “Асинхронность и HTTP-кэш (fetch, AbortController, ретраи)”</p>
<br><br><br><br><br><br>
<p align="right"><strong>Выполнил:</strong></p>
<p align="right">Студент 4 курса</p>
<p align="right">Группы АС-__</p>
<p align="right">Горкавчук Н.Н.</p>
<p align="right"><strong>Проверил:</strong></p>
<p align="right">________________</p>
<br><br><br><br><br>
<p align="center"><strong>Брест 2025</strong></p>

---

## Цель работы

* Закрепить работу с промисами и async/await, обработкой ошибок, таймаутами и отменой запросов.
* Понять основы HTTP-кэширования и увидеть эффект в DevTools.

---

## Вариант

**Список фильмов (OMDb API) с поиском и деталями.**

---

## Ход выполнения работы

### 1. Структура проекта

- `index.html` — основная страница приложения (форма поиска, список, детали, кнопка «Обновить»)
- `styles.css` — стилизация (dark UI / glassmorphism, skeleton loaders)
- `main.js` — логика на чистом JavaScript (fetch, retry/timeout/abort, кэш)

---

### 2. Подключение к API (OMDb)

В качестве источника данных используется публичный API OMDb:

- Загрузка списка по запросу: параметр `s`
- Пагинация: параметр `page`
- Фильтрация по году: параметр `y` (опционально)
- Детальная информация по `imdbID`: параметр `i`

> Важно: страницу необходимо открывать через локальный HTTP-сервер (например Live Server), а не через `file://`, иначе браузер блокирует запросы.

---

### 3. Реализация состояний интерфейса (UX)

В приложении реализованы состояния:

- `loading` — skeleton/spinner при загрузке списка и деталей
- `error` — вывод ошибки в UI, в т.ч. обработка сетевых ошибок
- `empty` — сообщение при отсутствии результатов

UX-улучшения:

- Дебаунс ввода (задержка перед поиском)
- Отмена предыдущего запроса при новом вводе (без “гонок”)
- Кнопка **«Обновить (без кэша)»** — принудительная перезагрузка

---

## 4. Описание кэш-подхода

### 4.1 Тип кэша

Используются два уровня клиентского кэша:

1) **In-memory Map**
```js
const memoryCache = new Map();
```

2) **localStorage с TTL** (для сохранения кэша между перезагрузками страницы)
```js
localStorage.setItem(prefix + key, JSON.stringify({ exp, value }));
```

### 4.2 Ключ кэша

Ключ формируется так, чтобы уникально описывать запрос:

- Для списка:
```text
omdb:list:q=<query>|y=<year>|p=<page>
```

- Для деталей:
```text
omdb:detail:<imdbID>
```

Это позволяет отдельно кэшировать:
- разные поисковые запросы
- разные страницы пагинации
- разные фильмы (детали)

### 4.3 TTL

Каждая запись имеет срок жизни:

```js
const TTL_MS = 3 * 60 * 1000; // пример: 3 минуты
```

Проверка валидности кэша:
- если TTL не истёк → данные берутся из кэша
- если истёк → выполняется новый запрос, кэш обновляется

### 4.4 Принудительное обновление (игнор кэша)

Кнопка «Обновить (без кэша)» делает перезагрузку данных даже при наличии свежих записей:

```js
loadList({ forceRefresh: true });
openDetail(id, { forceRefresh: true });
```

Также на уровне fetch используется режим:
```js
fetch(url, { cache: "no-store" });
```

### 4.5 Поведение в DevTools / сети

Ожидаемое поведение:

- Первый запрос → уходит в сеть → `200 OK` → данные сохраняются в кэш
- Повтор того же запроса (в пределах TTL) → данные берутся из кэша приложения → сетевого запроса может не быть
- Нажатие «Обновить (без кэша)» → принудительно уходит запрос в сеть → кэш перезаписывается

> Примечание: HTTP-кэш (304 Not Modified) зависит от заголовков ответа сервера. В данной работе основной акцент сделан на клиентский кэш (Map/localStorage) + демонстрация в Network по наличию/отсутствию запросов и параметру `cache: no-store`.

### 4.6 Преимущества подхода

- Быстрая повторная отрисовка без сети
- Меньше запросов к API
- Простой контроль актуальности через TTL
- Возможность принудительного “hard refresh”

---

## 5. Описание ретраев/таймаутов/отмены

### 5.1 Ретраи (повторные попытки)

При сетевой ошибке или ошибке ответа выполняются повторные попытки:

- Количество ретраев: `retries`
- Задержка: экспоненциальный backoff

Пример логики:
```js
while (attempt <= retries) {
  try {
    // fetch
    return data;
  } catch (err) {
    if (attempt >= retries) throw err;
    await sleep(backoffMs * 2 ** (attempt - 1));
  }
}
```

### 5.2 Таймаут

Ограничение времени ожидания реализовано через `AbortController`:

```js
const controller = new AbortController();
const timer = setTimeout(() => controller.abort(), timeoutMs);
```

Если ответ не пришёл за `timeoutMs`, запрос прерывается и выбрасывается `AbortError`.

### 5.3 Отмена устаревших запросов (AbortController)

При новом поиске отменяется предыдущий запрос списка:

```js
if (listController) listController.abort();
listController = new AbortController();
```

Аналогично при выборе другой карточки отменяется предыдущий запрос деталей.

Это предотвращает ситуацию, когда старый (медленный) ответ “перетирает” актуальные данные в UI.

---

## 6. Ссылка на публикацию

👉 Вставьте ссылку на GitHub Pages:  
**https://exage.github.io/WT_3/**

---

## Вывод

В ходе выполнения лабораторной работы был реализован клиент к OMDb API со списком фильмов и отображением детальной информации. Закреплены навыки работы с асинхронностью в JavaScript на основе `async/await`, реализованы обработка ошибок, повторные попытки запросов (retry) с экспоненциальной задержкой, таймауты ожидания и отмена устаревших запросов через `AbortController`. На практике изучены подходы к клиентскому кэшированию с TTL (in-memory Map и localStorage), а также продемонстрировано влияние кэша и принудительного обновления на количество сетевых запросов в DevTools.
