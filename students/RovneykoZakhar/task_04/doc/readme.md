# Лабораторная работа 04

## REST‑клиент SPA: список / детализация / CRUD, маршрутизация (без фреймворков)

## 📖 Описание

Одностраничное приложение (SPA) на чистом JavaScript с hash‑маршрутизацией.  
Реализованы экраны списка, детализации, создания и редактирования элементов, а также полный набор CRUD‑операций к учебному API (реальному или mock‑серверу).

---

## 🎯 Цели

- Реализовать простой SPA без использования фреймворков.
- Организовать маршрутизацию на основе `hashchange` и `load`.
- Добавить состояния загрузки, ошибок и пустых данных.
- Выполнить CRUD‑операции (GET, POST, PATCH/PUT, DELETE).
- Обеспечить удобный UX: подтверждение удаления, блокировка кнопки отправки во время запроса, валидация форм.
- Сохранять параметры поиска в hash и восстанавливать их при перезагрузке.

---

## 🛠 Задания

- **Маршруты (hash):**
  - `#/items` — список элементов
  - `#/items/:id` — детализация элемента
  - `#/new` — создание нового элемента
  - `#/items/:id/edit` — редактирование элемента

- **Состояния:**
  - `loading` — индикатор загрузки
  - `error` — отображение ошибок
  - `empty` — пустой список
  - уведомления об успешных операциях

- **CRUD:**
  - `GET` — список и деталь
  - `POST` — создание
  - `PATCH/PUT` — обновление
  - `DELETE` — удаление

- **UX:**
  - подтверждение удаления
  - блокировка submit во время отправки
  - валидация форм
  - сохранение параметров поиска в hash

---

## ⚙️ Минимальные технические требования

- Маршрутизатор на основе событий `hashchange` и `load`.
- Разделение на модули: `router`, `api`, `views`.
- Адаптивная вёрстка списка и форм.

---

## 📂 Артефакты

- Исходники (разделены по модулям).
- Демо‑ссылка.
- README: описание маршрутов, API, инструкции по запуску/mock‑серверу, скриншоты.

---

## 📊 Критерии оценивания (100 баллов)

- Семантика / UX — **20**
- Функциональность / CRUD / маршрутизация — **25**
- Качество интерфейса — **20**
- Качество кода / модульность — **15**
- Тесты / валидность — **10**
- Публикация и отчёт — **10**

**Бонусы (+ до 10):**

- Сохранение фильтров/поиска в hash и восстановление при перезагрузке.
- Предзагрузка данных (prefetch) при наведении/фокусе.
- Простая клиентская авторизация (token в header).

---

## 🚀 Запуск проекта

1. Установите зависимости:

   ```bash
   npm install
